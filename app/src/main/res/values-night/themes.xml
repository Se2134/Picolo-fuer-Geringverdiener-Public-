<resources>
    <!-- Base application theme. -->
    <style name="Theme.PicoloFuerGeringverdiener" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary_dark</item>
        <item name="colorPrimaryVariant">@color/primary</item>
        <item name="colorOnPrimary">@color/primary_content</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryVariant">@color/secondary_dark</item>
        <item name="colorOnSecondary">@color/secondary_content</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" >?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:background">@color/neutrals_background</item>
        <item name="android:textColor">@color/neutrals_copy</item>

        <item name="einstellungen">@drawable/einstellungen</item>
    </style>

    <style name="toolbar">
        <item name="titleTextColor">@color/neutrals_copy</item>
        <item name="android:background">@color/neutrals_foreground</item>
    </style>

    <attr name="einstellungen" format="reference"/>
</resources>